from django.conf.urls import patterns, include, url

# Uncomment the next two lines to enable the admin:
from django.contrib import admin
admin.autodiscover()

urlpatterns = patterns('',
    # Examples:
    # url(r'^$', 'eureka.views.home', name='home'),
    # url(r'^eureka/', include('eureka.foo.urls')),

    # Uncomment the admin/doc line below to enable admin documentation:
    # url(r'^admin/doc/', include('django.contrib.admindocs.urls')),

    # Uncomment the next line to enable the admin:
    # url(r'^admin/', include(admin.site.urls)),
    # url(r'^home/','index.views.home'),
    # url(r'^qingy/hello/$','qingy.views.hello'),
    # url(r'^blog/$','qingy.views.blog'),
    # url(r'^signup/$','webapp.views.contributer_signup'),
    # url(r'^home/$','webapp.views.home'),
      url(r'^login/$','webapp.views.userlogin'),
      url(r'^upload/$','panda.views.contributor_upload'),
      url(r'^user/review/$','panda.views.review'),
	
      
   
      
)
